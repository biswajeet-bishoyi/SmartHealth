services:
  # Backend Web Service (Node.js + Socket.IO + Express)
  - type: web
    name: smarthealthne-backend
    plan: free
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://smarthealthne-frontend.onrender.com

  # Frontend Static Web App (React + Vite + Tailwind + FontAwesome)
  - type: web
    name: smarthealthne-frontend
    plan: free
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://smarthealthne-backend.onrender.com
